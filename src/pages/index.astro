---
import Layout from "../layouts/Layout.astro";
import { CA, BUY_URL, TWITTER_URL, TELEGRAM_URL, DEXSCREENER_URL } from "../utils/utils";

---

<Layout
	title="HappyCoin - The Solana Memecoin for True Happiness"
	description="HappyCoin brings joy and prosperity to the Solana ecosystem. Join our community of happiness seekers and experience genuine fun in the crypto world."
	keywords="happycoin, memecoin, solana, cryptocurrency, happiness, joy, community, meme token, crypto investment, solana memecoin, positive vibes"
	canonicalURL=""
	robots="index, follow"
>
  <!-- seccion 1 -->

  <div class="min-h-screen bg-[#f7eb1b] flex flex-col items-center py-10">
    <h1 class="text-black text-4xl font-bold">
      just be happy
    </h1>

    <video src="/videohappy.mp4" autoplay muted loop class="w-96 h-96 mt-10"/>

    <h1 class="text-black text-center mt-8 text-6xl font-bold">
      happycoin
    </h1>

    <a href={BUY_URL} target="_blank" class="p-4 font-thin px-8 text-2xl mt-10 bg-[#fa7e01] rounded-[35px] byrd-medium-btn inline-block text-white hover:no-underline hover:scale-110 transition-all text-center"> 
      buy now
    </a>

    <div class="w-full gap-8 mt-6 flex justify-center">
      <a href={TWITTER_URL} target="_blank" class="hover:scale-125 transition-transform">
        <img src="/social/x-icon.svg" alt="x icon" class="w-10 h-10">
      </a>

      <a href={DEXSCREENER_URL} target="_blank" class="hover:scale-125 transition-transform">
        <img src="/social/dex-icon.svg" alt="dexscreener icon" class="w-10 h-10">
      </a>
    </div>

  </div>

  <!-- Nueva sección sobre HappyCoin -->
  <div class="min-h-screen bg-[#f7eb1b] flex flex-col items-center justify-center py-20 px-6">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="text-black text-5xl md:text-6xl font-bold mb-8 byrd-medium-btn">
        Welcome to HappyCoin
      </h2>
      
      <div class="rounded-3xl p-8 md:p-12 mb-8">
        <p class="text-black text-xl md:text-2xl leading-relaxed mb-6">
          In a world full of financial uncertainty, <span class="font-bold text-[#fa7e01]">HappyCoin</span> emerges as an oasis of joy in the Solana ecosystem.
        </p>
        
        <p class="text-black text-lg md:text-xl leading-relaxed mb-6">
          We're not just another memecoin. We're a community built on <span class="font-bold">positivity, prosperity, and genuine connections</span> in the crypto space.
        </p>
        
        <p class="text-black text-lg md:text-xl leading-relaxed mb-6">
          In the crypto meme industry, where volatility and chaos reign, HappyCoin represents <span class="font-bold text-[#fa7e01]">stability, community, and authentic joy</span> for all our holders.
        </p>
        
        <div class="grid md:grid-cols-3 gap-6 mt-12">
          <div class="rounded-2xl p-6 border border-white/10">
            <div class="text-4xl mb-4">😊</div>
            <h3 class="text-black font-bold text-xl mb-2">Real Happiness</h3>
            <p class="text-black/80 text-sm">We build wealth while cultivating authentic joy in our community.</p>
          </div>
          
          <div class="rounded-2xl p-6 border border-white/10">
            <div class="text-4xl mb-4">🚀</div>
            <h3 class="text-black font-bold text-xl mb-2">Solana Innovation</h3>
            <p class="text-black/80 text-sm">We leverage Solana's speed and efficiency to create unique experiences.</p>
          </div>
          
          <div class=" rounded-2xl p-6 border border-white/10">
            <div class="text-4xl mb-4">🌟</div>
            <h3 class="text-black font-bold text-xl mb-2">Community</h3>
            <p class="text-black/80 text-sm">More than holders, we're a family united by the pursuit of prosperity and joy.</p>
          </div>
        </div>
      </div>
      
      <p class="text-black text-lg md:text-xl font-medium">
          Join the HappyCoin community today. <br>
          <span class="text-[#fa7e01] font-bold">Where crypto meets genuine happiness and prosperity.</span>
        </p>
        
        <!-- Sección adicional -->
        <div class="mt-16 bg-white/10 backdrop-blur-sm rounded-3xl p-8 md:p-12 border border-black/20">
          <h3 class="text-black text-3xl md:text-4xl font-bold mb-6">Why Choose HappyCoin?</h3>
          
          <p class="text-black text-lg md:text-xl leading-relaxed mb-6">
            HappyCoin isn't just about financial gains - it's about building a sustainable ecosystem where every holder benefits from our collective success. Our tokenomics are designed to reward long-term believers while maintaining healthy market dynamics.
          </p>
          
          <p class="text-black text-lg md:text-xl leading-relaxed mb-6">
            Built on Solana's lightning-fast blockchain, HappyCoin offers near-instant transactions with minimal fees. This means more money stays in your pocket, and more happiness in your wallet.
          </p>
          
          <div class="grid md:grid-cols-2 gap-8 mt-8">
            <div class="text-center">
              <div class="text-5xl mb-4">💎</div>
              <h4 class="text-black font-bold text-xl mb-2">Diamond Hands Rewards</h4>
              <p class="text-black/80 text-sm">Long-term holders receive exclusive benefits and rewards for their loyalty to the HappyCoin ecosystem.</p>
            </div>
            
            <div class="text-center">
              <div class="text-5xl mb-4">⚡</div>
              <h4 class="text-black font-bold text-xl mb-2">Lightning Fast</h4>
              <p class="text-black/80 text-sm">Powered by Solana's high-performance blockchain for instant transactions and minimal fees.</p>
            </div>
          </div>
        </div>
        
        <div class="mt-12 text-center">
          <p class="text-black text-xl md:text-2xl font-bold mb-4">
            Ready to spread some happiness?
          </p>
          <p class="text-black/80 text-lg mb-8">
            Join thousands of happy holders who have already discovered the joy of HappyCoin.
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <a href={BUY_URL} target="_blank" class="byrd-medium-btn bg-[#fa7e01] hover:bg-[#e56d00] text-white px-8 py-4 rounded-full font-bold text-lg transition-all duration-300 hover:scale-105">
              Buy HappyCoin Now
            </a>
            
            <a href={TELEGRAM_URL} target="_blank" class="byrd-medium-btn bg-black hover:bg-gray-800 text-white px-8 py-4 rounded-full font-bold text-lg transition-all duration-300 hover:scale-105">
              Join Community
            </a>
          </div>
        </div>
    </div>
  </div>

    <!-- Modal para ver imágenes -->
    <div id="imageModal" class="fixed inset-0 bg-black/80 z-50 hidden items-center justify-center p-4">
      <div class="relative max-w-4xl max-h-full">
        <button id="closeModal" class="absolute -top-12 right-0 text-white text-4xl hover:text-gray-300 transition-colors">
          ×
        </button>
        <img id="modalImage" src="" alt="Meme" class="max-w-full max-h-full object-contain rounded-lg">
      </div>
    </div>





<style>
  ul {
    list-style-type: none;
    padding-left: 0;
  }

  li {
    padding: 0.75rem 0;
  }

  .hero-image {
    animation: slideUp 1s ease-out forwards;
    transform: translateY(100%);
  }

  @keyframes slideUp {
    to {
      transform: translateY(0);
    }
  }

  .fancy-frame {
    border: 8px solid white;
    border-radius: 12px;
    box-shadow: 
      0 0 0 2px #d72d0e,
      0 0 15px rgba(0,0,0,0.3),
      inset 0 0 15px rgba(0,0,0,0.3);
    transition: transform 0.3s ease;
  }

  .fancy-frame:hover {
    transform: scale(1.02);
  }

  .gradient-text {
    background: linear-gradient(to right, #ffffff, #ffd700);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 2px 10px rgba(255,255,255,0.2);
  }
  
  .byrd-medium-btn {
    font-family: 'ByrdMedium', 'Byrd Fallback', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif !important;
  }
</style>

<style>
    .scroll-container {
      position: relative;
      overflow: hidden;
    }
    
    #scrollContent {
      position: relative;
      transition: transform 0.1s ease-out;
    }
</style>
    
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const scrollContent = document.getElementById('scrollContent');
    if (!scrollContent) return;
    const container = scrollContent.parentElement;
    let lastScrollY = window.scrollY;
    let ticking = false;
    let containerTop = container.offsetTop;
    let containerBottom = containerTop + container.offsetHeight;
    let maxScroll;

    function updatePosition() {
      if (window.innerWidth < 768) return;
      
      if (!scrollContent) return;
      
      const scrollY = window.scrollY;
      const windowHeight = window.innerHeight;
      const contentHeight = scrollContent.offsetHeight;
      maxScroll = contentHeight - windowHeight;

      if (scrollY >= containerTop && scrollY <= containerBottom - windowHeight) {
        const scrollPercent = (scrollY - containerTop) / (containerBottom - containerTop - windowHeight);
        const translateY = Math.min(maxScroll * scrollPercent, maxScroll);
        
        scrollContent.style.transform = `translateY(${translateY}px)`;
      }
      
      lastScrollY = scrollY;
      ticking = false;
    }

    window.addEventListener('scroll', () => {
      if (!ticking) {
        window.requestAnimationFrame(updatePosition);
        ticking = true;
      }
    });

    window.addEventListener('resize', () => {
      containerTop = container.offsetTop;
      containerBottom = containerTop + container.offsetHeight;
      
      if (window.innerWidth < 768) {
        scrollContent.style.transform = 'translateY(0)';
      } else {
        updatePosition();
      }
    });

    updatePosition();
  });
  
  // Modal functionality for memes
   document.addEventListener('DOMContentLoaded', () => {
     const modal = document.getElementById('imageModal');
     const modalImage = document.getElementById('modalImage') as HTMLImageElement;
     const closeModal = document.getElementById('closeModal');
     const memeItems = document.querySelectorAll('.meme-item');
     
     // Open modal when clicking on meme items
     memeItems.forEach(item => {
       item.addEventListener('click', () => {
         const imageUrl = item.getAttribute('data-image');
         if (modalImage && imageUrl) {
           modalImage.src = imageUrl;
         }
         modal?.classList.remove('hidden');
         modal?.classList.add('flex');
         document.body.style.overflow = 'hidden';
       });
     });
    
    // Close modal
     const closeModalFunction = () => {
       modal?.classList.add('hidden');
       modal?.classList.remove('flex');
       document.body.style.overflow = 'auto';
     };
     
     closeModal?.addEventListener('click', closeModalFunction);
     
     // Close modal when clicking outside the image
      modal?.addEventListener('click', (e) => {
        if (e.target === modal) {
          closeModalFunction();
        }
      });
      
      // Close modal with ESC key
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && modal && !modal.classList.contains('hidden')) {
          closeModalFunction();
        }
      });
    });
</script>